<configuration>
    <property name="LOG_DIR" value="/data/data/com.blueeagle.realm/files/log" />

    <!-- Create a logcat appender -->
    <appender name="logcat" class="ch.qos.logback.classic.android.LogcatAppender">
        <encoder>
            <pattern>%d{dd MMM yyyy;HH:mm:ss.SSS} [%thread] %-5level %class{36} line %line
                [%caller{1}] %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Create a file appender for TRACE-level messages -->
    <appender name="TraceLog" class="ch.qos.logback.core.FileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>TRACE</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>

        <file>${LOG_DIR}/trace.log</file>

        <encoder>
            <pattern>%d{dd MMM yyyy;HH:mm:ss.SSS} [%thread] %-5level %class{36} line %line
                [%caller{1}] %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Create a file appender for DEBUG-level messages -->
    <appender name="DebugLog" class="ch.qos.logback.core.FileAppender">
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>DEBUG</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>

        <file>${LOG_DIR}/debug.log</file>

        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Write DEBUG messages from class Contact to its own log -->
    <logger name="Model.Contact" level="DEBUG">
        <appender-ref ref="DebugLog" />
    </logger>

    <logger name="com.blueeagle.realm.MainActivity" level="TRACE">
        <appender-ref ref="TraceLog" />
    </logger>

    <!-- Write DEBUG (and higher-level) messages to logcat -->
    <root level="DEBUG">
        <appender-ref ref="logcat" />
    </root>
</configuration>